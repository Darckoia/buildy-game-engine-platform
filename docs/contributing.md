# Contributing

1. Create a branch from the latest default branch.
2. Keep pull requests focused and small.
3. Run lint, typecheck, tests, and build locally before opening a PR.
4. Use `.env.example` as the template for local `.env` files.
5. Never commit secrets; use repository secrets in CI/CD.
