{
  "name": "@buildy/api",
  "version": "0.1.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "dev": "tsx src/index.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src --ext .ts"
  },
  "dependencies": {
    "@buildy/auth": "workspace:*",
    "@buildy/contracts": "workspace:*",
    "@buildy/errors": "workspace:*",
    "@buildy/types": "workspace:*",
    "express": "4.21.2",
    "cors": "2.8.5"
  },
  "devDependencies": {
    "@types/cors": "2.8.17",
    "@types/express": "4.17.21"
  }
}
